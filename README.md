# Pull Request Code Review System API

Command to start dockerize application server:
```
docker compose up
docker compose up -d (detach mode) (in background)
```

Command to stop dockerize application server:
```
docker compose down
```